import 'package:flutter/material.dart';

class ListingsUserPage extends StatelessWidget {
  const ListingsUserPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ListingUserPage"),
    );
  }
}
